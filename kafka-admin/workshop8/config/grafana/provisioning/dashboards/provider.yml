apiVersion: 1
providers:
  - name: Kafka Training
    folder: Kafka Training
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: $KAFKA_TRAINING_DASHBOARDS
